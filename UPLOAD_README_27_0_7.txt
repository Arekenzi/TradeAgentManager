UPLOAD TO GITHUB — Trade Agent Manager v27.0.7

1. Upload ALL files from the ZIP to the ROOT of Arekenzi/TradeAgentManager.
2. Replace existing files with the same names.
3. Keep only lowercase index.html. Delete old Index.html if it still exists.
4. FIRESTORE_RULES.txt may remain in the repository; it is not part of this runtime ZIP.
5. Wait for GitHub Pages publication.
6. Browser: hard refresh.
7. iPhone Home Screen/PWA: fully close the app from the app switcher and reopen it.
   If an old build remains cached, remove the Home Screen shortcut and add it again once.

v27.0.7 fixes the iPhone/PWA Login MASTER T by embedding native SVG animation directly in the logo, and optically re-centers the login shell.
